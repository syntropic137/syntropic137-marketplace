{
  "triggers": [
    {
      "name": "on-release-created",
      "description": "Prepare release notes when a new release is created",
      "event": "release",
      "conditions": [
        {
          "field": "action",
          "operator": "in",
          "value": ["created", "published"]
        }
      ],
      "input_mapping": {
        "repository": "repository.full_name",
        "tag": "release.tag_name",
        "release_name": "release.name",
        "draft": "release.draft"
      }
    }
  ]
}
